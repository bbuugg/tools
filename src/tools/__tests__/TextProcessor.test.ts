import { describe, it, expect } from 'vitest'
import TextProcessor from '../TextProcessor.vue'

describe('TextProcessor', () => {
  it('should be able to be imported without errors', () => {
    expect(TextProcessor).toBeTruthy()
  })
})
