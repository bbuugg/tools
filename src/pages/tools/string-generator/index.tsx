import Tool from "./Tool";

const title = '字符串生成器';
const description = 'UUID、ULID、随机字符串、密码生成';

export default function StringToolsPage() {
  return <Tool title={title} description={description} />
}
