import Tool from "./Tool";

const title = 'WebSocket 调试';
const description = '连接 WebSocket 服务器，发送和接收消息';

export default function WsToolPage() {
  return <Tool title={title} description={description} />
}
