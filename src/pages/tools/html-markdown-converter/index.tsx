import Tool from "./Tool";

const title = 'HTML / Markdown 互转';
const description = 'Markdown 与 HTML 之间双向转换';

export default function HtmlMarkdownConverterPage() {
  return <Tool title={title} description={description} />
}
