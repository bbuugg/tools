import Tool from "./Tool";

const title = '批量文件重命名';
const description = '序号、替换、大小写、插入、截取等多种重命名方式';

export default function FileRenamerPage() {
  return <Tool title={title} description={description} />
}
