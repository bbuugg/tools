import Tool from "./Tool";

const title = "颜色选择器";
const description = "HEX / RGB / CMYK 互转、图片取色与颜色预览工具";

export default function ColorPickerPage() {
  return <Tool title={title} description={description} />
}
