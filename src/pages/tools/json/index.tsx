import Tool from "./Tool";

const title = 'JSON 工具箱';
const description = '格式化、转换、提取 JSON 数据的集成工具';

export default function JsonToolsPage() {
  return <Tool title={title} description={description} />
}
