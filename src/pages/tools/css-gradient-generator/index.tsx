import Tool from "./Tool";

const title = 'CSS 渐变生成器';
const description = '可视化生成线性/径向渐变 CSS 代码';

export default function CssGradientGeneratorPage() {
  return <Tool title={title} description={description} />
}
