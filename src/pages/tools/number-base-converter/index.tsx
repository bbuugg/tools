import Tool from "./Tool";

const title = '进制转换器';
const description = '支持 2-36 进制之间的相互转换';

export default function NumberBaseConverterPage() {
  return <Tool title={title} description={description} />
}
