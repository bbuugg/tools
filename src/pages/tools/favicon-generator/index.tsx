import Tool from "./Tool";

const title = 'Favicon 生成器';
const description = '上传图片裁剪并生成多种尺寸的 Favicon';

export default function FaviconGeneratorPage() {
  return <Tool title={title} description={description} />
}
