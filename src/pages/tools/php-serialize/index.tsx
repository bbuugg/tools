import Tool from "./Tool";

const title = 'PHP Serialize 工具';
const description = 'PHP serialize/unserialize 在线工具，支持序列化和反序列化 PHP 格式数据';

export default function PhpSerializePage() {
  return <Tool title={title} description={description} />
}
