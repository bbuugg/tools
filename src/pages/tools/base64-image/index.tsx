import Tool from "./Tool";

const title = 'Base64 图片转换';
const description = 'Base64 与图片互转工具';

export default function Base64ImagePage() {
  return <Tool title={title} description={description} />
}
